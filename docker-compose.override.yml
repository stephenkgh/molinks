services:
  db:
    restart: "no"

  web:
    restart: "no"
    build:
      context: .
      dockerfile: dockerfile-web-dev
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/project
